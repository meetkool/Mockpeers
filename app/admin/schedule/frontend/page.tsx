import { InterviewSchedulePage } from "../../components/InterviewSchedulePage";

export default function FrontendSchedulePage() {
  return (
    <InterviewSchedulePage
      interviewType="FRONTEND"
      title="Frontend Schedule Management"
      description="Manage Frontend practice sessions (Beta)"
      iconName="Monitor"
    />
  );
}
