import { InterviewSchedulePage } from "../../components/InterviewSchedulePage";

export default function SQLSchedulePage() {
  return (
    <InterviewSchedulePage
      interviewType="SQL"
      title="SQL Schedule Management"
      description="Manage SQL practice sessions (Beta)"
      iconName="Database"
    />
  );
}
